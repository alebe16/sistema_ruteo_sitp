from setuptools import setup, find_packages

setup(
    name="sistema_ruteo_sitp",
    version="0.1",
    packages=find_packages(),
    package_dir={'': '.'},
    install_requires=[],  # Agrega dependencias si las tienes
)
